package com.codegnan.java8enchancements.StreamAPI;

public class ReduceDemo {
	
	// syntax : 
	 // reduce (identity,(a,b)-> a operation b);
	// - identity : the starting value 
	// - (a,b)-> a operation b :
	// This is a lambda expression
	// a-> accumulated result(previous value)
	// b-> current element from the stream
	// result = result+nextElement.


}
